# ISP Gateway IP
ip addr add 203.0.113.1/30 dev eth0

# Simulated Public Target (Cloudflare DNS as example)
ip addr add 1.1.1.1/32 dev lo

# Route back to the Edge Router's Public IP
ip route add 203.0.113.2/32 dev eth0